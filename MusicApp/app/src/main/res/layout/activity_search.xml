<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Button
        android:id="@+id/buy_music"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="buyMusic"
        android:text="Buy Music" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="20sp"
        android:text="This app would parse song titles, artists, albums, and album art using discogs.com's api, then put each string within a list layout and have every song lead to an intent that brings up an activity which makes the song purchasable."/>

</LinearLayout>