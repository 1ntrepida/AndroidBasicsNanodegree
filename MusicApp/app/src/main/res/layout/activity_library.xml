<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Show Details"
        android:onClick="showDetailsMusic"
        android:id="@+id/show_details_music"
        />

    <Button
        android:id="@+id/now_playing"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="nowPlaying"
        android:text="Now Playing" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="20sp"
        android:text="Shown in the same layout as the search activity, all purchased songs will be added here and will be listed by artist. Clicking each song will give the user an option to view the song details. At the bottom of the page, there will be a now playing button that will take them to that activity."/>

</LinearLayout>